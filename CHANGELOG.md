# 変更履歴

公開サイト https://rma-lab.github.io/kaken-atlas/ と生成スクリプトの変更を記録する。
版は `vX.Y`。**Y は機能追加・UI 変更**、**X は地図データ（埋め込み・UMAP 座標）の更新など見え方が根本的に変わる変更**。
各版は git タグと GitHub Release に対応する。日付は公開日。

## [Unreleased]

### サイト
- 更新直後にスマホの 2D で「読み込みに失敗しました（Length out of range of buffer）」が出る問題を修正。HTML はネットワーク優先、
  データはキャッシュ優先で配信しているため、新しい HTML と古い Service Worker の保存データ（色データのない points.bin）が
  一度だけ組み合わさっていた。データの URL に内容ハッシュ（`?v=`）を付けて古い保存に当たらないようにし、長さが合わない場合は
  1 回だけ再取得する

## [v1.3] — 2026-09-11

点の色を公式区分から「研究内容由来の連続色」へ。球面の縞の修正、区分なしの既定表示、技術ノートの 3 分割。

### サイト
- **点の色を「研究内容から導いた連続色」に変更**（大区分の 11 色から置き換え。大区分の色との切替は設けない）。
  768 次元の埋め込み空間に閉じた輪（弾性リング、198 ノード）を張り、各課題が輪のどこに最も近いかを色相に、
  周囲の合意度を彩度（下限を高く取る）にする。区分なし・複数の課題にも内容に応じた色が付く。
  色相の向きは従来の 11 色に揃えているので見慣れた対応（医歯薬＝赤〜橙、人文社会＝赤紫、数物＝青…）は残る。
  ヘッダー（スマホは羅針盤メニュー）の「色の見方」に色相環 12 方位の特徴キーワードを示す凡例。
  カードとツールチップの縁・見出しは点自身の色。大区分の凡例・シートの色見本は所属課題の平均色（環境学のように
  内容が散在する区分は灰色寄り）。大区分の表示切替は絞り込み機能としてそのまま残す。
  色表は `scripts/compute_textcolor.py` が生成し、`points.bin` の末尾に RGB 各 1 バイトで同梱（+0.6MB）
- 球面ビューを縮小したときに出ていた同心の縞（点と裏側を隠す球の深度バッファ衝突。ブラウザの深度バッファは 16 ビット）を修正。近クリップ面を視距離に応じて遠ざける方式（形状の差し替えなし、引っかかりも出ない）。点の半径の散らしも縮小

- 「区分なし」（特別研究員奨励費など審査区分の体系外の課題、19,564 件）を既定で表示。最下層に描く（当初は薄いグレー。同版の連続色化により内容に応じた色が付く）

### 文書
- 技術ノートを 3 部に分割: `doc/method.md`（方法と可視化の設計）、`doc/web.md`（サイトの実装と運用）、`doc/roadmap.md`（今後の設計メモ）。地図の読み方の原則（空白の解釈、距離の測り方）と、所要時間としての距離の定義を追記

## [v1.2] — 2026-09-09

課題ごとの URL、端末への保存（Service Worker）、選択の追随とフライ・トゥ。PC も点のクリックでカードを出す方式に。

### サイト
- Service Worker（`docs/sw.js`、生成物）を導入。取得した詳細データ・座標・Plotly を端末に保存し、再訪時は通信なしで開く（オフラインでも動く）。データが更新されると自動で古い保存を捨てる。HTML はネットワーク優先
- 右下の「全データ読み込み完了」は 2 秒で消す（従来 4 秒）。端末保存から一瞬で読み込めた場合は表示しない。「（検索が使えます）」の文言を削除
- **課題ごとの URL**: `map2d/?award=課題番号`（3D・球面も同じ形）で開くと、その課題にズーム／カメラを向けてリングとカードを表示。該当する詳細データ 1 片だけ読むので全件の読み込み完了を待たない
- カードにリンクのコピーと共有のアイコンボタン（共有は対応端末のみ。コピー後は 1.5 秒チェック印に）。カードを開くとアドレスバーの URL もその課題のものになる
- **フライ・トゥ**: URL や検索結果で課題を開くと、全体図の上でリングとカードを出してから 1.2 秒かけて周辺が見える倍率（2D は ±2.5、3D は注視点に寄る、球面は正面へ大円に沿って回る）まで連続的に寄る
- カードに「全体 ⇄ 周辺」の切替ボタン（球面は「この点を正面に」）
- **地図を動かしても選択は消えない**: リングは点に追随（球面の裏側に回ったら隠す）。PC のカードは点に付いて動き、スマホのカードは固定。解除は空白のタップ／クリックと ×、Esc
- **PC でも点のクリックで詳細カード**（KAKEN はカードのクリックで開く。従来の「クリックで即 KAKEN」から変更）。検索結果のクリックも PC でカードを出す

## [v1.1] — 2026-09-09

主にスマホでの使い勝手と、ビュー間のデータ共有。

### サイト
- 詳細データ（タイトル・キーワード等）を課題番号順の 1 セット `docs/shards/` にまとめて 3 ビューで共有。ビューを切り替えても再読み込みしない（ブラウザキャッシュが効く）。リポジトリの詳細データは 156MB → 52MB
- 検索欄の右端に × ボタン（検索語・地図上の強調・結果窓をすべて消す）。結果窓の外をクリック／タップすると結果窓だけ閉じ、入力欄に戻ると再表示（3 ビュー共通）
- 検索結果をタップ／クリックしたとき: タッチ端末は結果窓を閉じてから点を表示。3D・球面はカメラをその点に向け（球面は正面に、3D は注視点を移して寄る）、点の実際の画面位置にリングとカードを出す。PC の 3D・球面もカメラが動くようになった（従来は無反応）
- スマホで検索欄にフォーカスしたときの画面の自動拡大を抑止（タッチ端末では入力欄の文字を 16px に。iOS Safari の仕様対策）
- スマホのヘッダーが幅に収まるように: 検索欄が先に縮む（ボタンははみ出さない）、「操作」ボタンは羅針盤メニューの末尾「操作の説明」へ移動
- スマホの「種目」を「大区分」と同じボトムシートに統一（行タップで表示切替、すべて表示／非表示。大区分の行だけ色の丸付き）。「のみ」ボタンは廃止し、一度すべて非表示にしてから必要なものを表示する方式に
- スマホの詳細カードを画面の下端に（以前あった下部の大区分ボタン分の余白 64px が残っていたのを 12px に）。上側に出すのは、点のリングが下端カードに実際に掛かるときだけ（安全領域は実測）
- スマホの 3D・球面で拡大しているときの回転が速すぎる問題: 1 本指回転の感度を「視点から面（球面）／注視点（3D）までの距離」に比例させ、寄るほど弱く。慣性も同じ感度に

## [v1.0] — 2026-09-08

初版として公開中の状態。2026-09-04 の公開から 09-08 までの改良を含む。

### 地図データ
- 対象: 2019–2025 年度開始の科研費採択課題 206,078 件（採択時概要あり・不採択除く）
- 埋め込み: cl-nagoya/ruri-v3-310m（768 次元、L2 正規化）。テキスト＝タイトル＋キーワード＋採択時概要
- 配置: UMAP（cosine, n_neighbors=15, seed=42）。2D/3D は min_dist=0.1、球面は haversine・min_dist=0.0・spread=0.3
- 配色: 大区分 11 色の意味順色相環。通称は正典（審査区分表）どおり C=工学、D=応用物理・材料工学、E=化学（09-07 修正）

### サイト
- 入口ページ: 点群を背景にした明背景デザイン、2D／3D／球面の 3 ビューへの入口
- 3 ビュー共通: タイトル・キーワード・課題番号検索、大区分の表示切替、点から KAKEN 課題ページへ
- 2D: 種目フィルタ、なげなわ／矩形選択の集計（件数・区分内訳・頻出キーワード）
- 3D／球面: 自由回転、球面は端のない地球儀
- PC: ホバーで概要、クリックで KAKEN。ドラッグ・ホイール中は吹き出しを消す
- タッチ端末: タップ→カード→カードのどこでも KAKEN。3D／球面は自前のカメラ制御（1 本指回転・2 本指ピンチ）、回転の慣性
- ヘッダー左の羅針盤アイコンからトップと他ビューへ移動（ウェブアプリモード対応）
- PWA（ホーム画面追加）、羅針盤アイコン、OGP 画像、JSON-LD・sitemap・robots・llms.txt
- 二段階読み込み（座標 → 詳細シャード）、GoatCounter による閲覧計測

### 文書
- README（利用規程・ライセンス・引用）、開発者向け技術ノート `doc/method.md`（設計の理由・参考文献）
