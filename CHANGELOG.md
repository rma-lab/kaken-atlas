# 変更履歴

公開サイト https://rma-lab.github.io/kaken-atlas/ と生成スクリプトの変更を記録する。
版は `vX.Y`。**Y は機能追加・UI 変更**、**X は地図データ（埋め込み・UMAP 座標）の更新など見え方が根本的に変わる変更**。
各版は git タグと GitHub Release に対応する。日付は公開日。

## [Unreleased]

### サイト
- **地名を既定でオフに**（地図の第一印象を「人が線を引く前の地形」に保つため）。ツールバーに「地名」ボタン（押すと点灯）を置き、
  「操作」のチェックと連動。設定は端末に残る。初めて開いたときだけ、ボタンの下に「地名を表示できます」の小さな案内を出す
  （閉じるか 30 秒で消え、以後は出さない）。2D はオンにすると全体表示でも粗い階層の地名を出す（押した直後に何も起きないのを避ける）

## [v1.7] — 2026-09-18

地名を、周辺の課題の研究概要を LLM が読んで付けた一段抽象的な名前に（2D・球面）。

### サイト
- **地名を LLM で一段抽象化**（2D・球面）。キーワード地名は著者キーワードから選ぶため専門語になりがちだった（「膵癌・腫瘍免疫」）。
  山域ごとに、中心重みで無作為抽出した課題の本文（タイトル＋キーワード＋概要。粗い階層 25 件、細かい階層 15 件）を Claude Opus 5 に
  読ませ、領域全体を包含する 4〜12 文字の名詞句を付けた（「がんの分子病態と治療開発」「量子物性と新奇材料」「網膜と視覚の疾患科学」）。
  細かい階層には親の名前を渡して親の中での違いを出す。英語名と根拠も保存。長い名前は「と・の」の位置で 2 行に折る。
  `PLACENAMES_SOURCE=keywords` で従来の表示に戻せる
- 「操作」の地名の説明を LLM の名前に合わせて修正（「周辺の課題の研究概要を AI が読んで付けた名前」）

### 文書・スクリプト
- LLM 命名 `scripts/name_places_llm.py`（抽出・プロンプト・検査・やり直し）。技術ノート（method.md §12、web.md、roadmap.md）、紹介資料の文言と拡大図、Hugging Face の説明ページを LLM の地名に合わせて更新

## [v1.6] — 2026-09-17

球面地図にもキーワード地名。埋め込みと座標のデータセットを Hugging Face で公開。A4 両面の紹介資料。

### サイト
- **球面地図にもキーワード地名**。球面の配置は 2D とは別なので、球面上の密度から峰と山域を求め直した（ほぼ均等なフィボナッチ格子の上で
  平滑化・峰の検出・分水嶺）。既定の表示で大きな領域の名前（45 か所、1,000 件以上）を出し、近づくと細かい階層（159 か所）に切り替わる。
  回すと地名が縁から現れて裏へ消える。縁に近い地名は薄くし、正面から外れたものは出さない。球面には内部に小さな峰を持たない
  裾野の広い山が 2 つあるので（物性・材料、代謝・循環器、各 2 万件）、大きすぎる山域は位置で区画に分けて区画ごとに名付ける。
  3D には付けない（奥行きで地名が重なり、どの塊を指すか分からなくなるため）。球面は点が詰まって見えるので、地名は少なめ
  （画面に粗い階層 7 個・細かい階層 14 個まで、スマホはその 7 割）、文字も 2D より一回り小さくした
- トップページの脚注と llms.txt に、Hugging Face のデータセットへのリンクを追加

### 文書・スクリプト
- 球面の地名の計算 `scripts/compute_placenames_sphere.py`、検証図 `scripts/plot_placenames.py --sphere`（6 方向の正射影）。回帰試験に球面の 10 項目を追加（計 71 項目）
- **データセットを Hugging Face で公開**: [rma-lab/kaken-atlas-embeddings](https://huggingface.co/datasets/rma-lab/kaken-atlas-embeddings)（埋め込み 768 次元 × 206,078 件、2D・3D・球面の座標、点の色、キーワード地名、最近傍距離。概要本文・研究者名・機関は含めない。CC BY 4.0）。組み立ては `scripts/build_hf_dataset.py`、説明ページの原稿は `scripts/hf/dataset_card.md`
- 紹介資料（A4 両面の PDF。表＝概要と使い方、裏＝技術の詳細）を `doc/flyer/` に追加（RA 協議会 2026 年 9 月の配布用）

## [v1.5] — 2026-09-17

2D にキーワード地名（研究が密集している場所ごとに、周辺の課題のキーワードから作った地名。公式区分は使わない）。地図サイトの回帰試験一式とリファクタリング。

### サイト
- **2D にキーワード地名を追加**。研究が密集している場所ごとに、周辺の課題のキーワードから作った地名を置く。方法: 課題の密度を地形と見て、密度の峰ごとに「山域」（峰から裾野まで、谷で隣と分かれるひとまとまり。
  反転した密度の分水嶺で切る）を作り、山域に属する課題のキーワードから特徴語 2 語（頻度でなく集中度。峰に近い課題ほど重く数える）を
  峰の位置に置く。公式区分は使わない。全体表示では出さず、2 倍以上の拡大で粗い階層（42 か所）、3 倍以上で細かい階層（129 か所）に
  切り替わる。件数の多い峰を優先して重なる地名は省き、描画領域の外には出さない。点の操作は素通し。「操作」（スマホは羅針盤メニューの
  「操作の説明」）の先頭で表示を切り替えられ、設定は端末に残る。3D・球面は未対応（`scripts/compute_placenames.py`、
  `build_web_map.py` の `PLACENAMES=0` で外せる）
- なげなわ集計の頻出キーワードを上位 15 語から 10 語に
- 大区分の凡例・シートの色見本で「複数」「区分なし」は平均色でなく灰色に（分野ではないため）

### 文書・スクリプト
- リファクタリング（動作は同一。各段階で回帰試験 46 項目・生成物のハッシュ 14 項目・スクリーンショット 9 枚が一致）:
  地図の HTML/JS と Service Worker を `scripts/web/` の実ファイルへ分離、`build_web_map.py` の main を段階ごとの関数に分割、
  JS の setupUI（1,400 行の閉包）を script スコープの状態変数・関数と節ごとの init 関数に平坦化（var の巻き上げの罠が構造的に消滅）
- 地図サイトの回帰試験一式を `tests/web/` に追加（ヘッドレス Chrome、46 項目、約 2 分。リファクタリングの安全網）。生成物のハッシュ照合（`artifacts.py`）と基準スクリーンショットの画素差分（`shots.js`）も
- キーワード地名の計算 `scripts/compute_placenames.py` と検証図 `scripts/plot_placenames.py`（階層ごとに `reports/figures/placenames_s<σ>.png`）。
  回帰試験に地名の 15 項目を追加（計 61 項目）。scikit-image を依存に追加

## [v1.4] — 2026-09-11

なげなわ集計の作り直し（固まる問題の解消、囲った範囲の追随表示）と、v1.3 直後の不具合修正（更新直後の読み込み失敗、2D ピンチのカクつき）。

### サイト
- 2D のなげなわで囲った後に地図が固まる／動かせなくなる問題を修正（v1.2 から。スマホで顕著）。原因は 2 つ。Plotly の選択機能が
  囲った後に全トレースの選択状態を再計算する（点ごとの色配列では 20 万点 × 174 本で数秒）ことと、操作モードが「なげなわ」のまま
  残り集計パネルがスマホでは地図を覆っていたこと。**なげなわを自前実装に置き換え**（画面座標の多角形で点の内外を判定、20 万点で
  数十 ms。点の強調表示はせず輪郭線と集計パネルで示す）、囲い終えたら自動で移動モードに戻す。ツールバーは Plotly の矩形選択・
  ズームボックスを外し「囲って集計」ボタンに。パネルは狭幅では高さ 40% まで、「閉じる ×」ボタン。囲った範囲は地図の座標で保持し、外側に薄い膜をかけて内側だけ鮮やかに残す（地図を動かしても追随。SVG の重ね描きなので軽い）。パネルは閉じるまで残る

### 文書・スクリプト
- README・CLAUDE.md・技術ノートを v1.3 の仕様（連続色、データ URL の版、2D の縦横比の自前管理、地名の方針）に合わせて更新。
  種目の比較（挑戦的研究）の補助分析 `scripts/plot_category_contrast.py`・`scripts/knn_dist_by_category.py` と知見を method.md §11 に記載

### サイト
- 2D のピンチ・ズームが v1.3 でカクつく問題を修正。縦横比を Plotly の `scaleanchor` に任せていると範囲変更のたびに全再計算の経路に
  入り、点ごとの色配列で 1 回 190ms かかっていた（v1.2 でも 32ms）。`scaleanchor` を外し、縦横比は自前で保つ（初期表示は全体が収まる
  等スケール、範囲変更後にずれていれば y だけ補正、自動範囲に戻ったときは決め直し）。1 回 33ms に
- 更新直後にスマホの 2D で「読み込みに失敗しました（Length out of range of buffer）」が出る問題を修正。HTML はネットワーク優先、
  データはキャッシュ優先で配信しているため、新しい HTML と古い Service Worker の保存データ（色データのない points.bin）が
  一度だけ組み合わさっていた。データの URL に内容ハッシュ（`?v=`）を付けて古い保存に当たらないようにし、長さが合わない場合は
  1 回だけ再取得する

## [v1.3] — 2026-09-11

点の色を公式区分から「研究内容由来の連続色」へ。球面の縞の修正、区分なしの既定表示、技術ノートの 3 分割。

### サイト
- **点の色を「研究内容から導いた連続色」に変更**（大区分の 11 色から置き換え。大区分の色との切替は設けない）。
  768 次元の埋め込み空間に閉じた輪（弾性リング、198 ノード）を張り、各課題が輪のどこに最も近いかを色相に、
  周囲の合意度を彩度（下限を高く取る）にする。区分なし・複数の課題にも内容に応じた色が付く。
  色相の向きは従来の 11 色に揃えているので見慣れた対応（医歯薬＝赤〜橙、人文社会＝赤紫、数物＝青…）は残る。
  ヘッダー（スマホは羅針盤メニュー）の「色の見方」に色相環 12 方位の特徴キーワードを示す凡例。
  カードとツールチップの縁・見出しは点自身の色。大区分の凡例・シートの色見本は所属課題の平均色（環境学のように
  内容が散在する区分は灰色寄り）。大区分の表示切替は絞り込み機能としてそのまま残す。
  色表は `scripts/compute_textcolor.py` が生成し、`points.bin` の末尾に RGB 各 1 バイトで同梱（+0.6MB）
- 球面ビューを縮小したときに出ていた同心の縞（点と裏側を隠す球の深度バッファ衝突。ブラウザの深度バッファは 16 ビット）を修正。近クリップ面を視距離に応じて遠ざける方式（形状の差し替えなし、引っかかりも出ない）。点の半径の散らしも縮小

- 「区分なし」（特別研究員奨励費など審査区分の体系外の課題、19,564 件）を既定で表示。最下層に描く（当初は薄いグレー。同版の連続色化により内容に応じた色が付く）

### 文書
- 技術ノートを 3 部に分割: `doc/method.md`（方法と可視化の設計）、`doc/web.md`（サイトの実装と運用）、`doc/roadmap.md`（今後の設計メモ）。地図の読み方の原則（空白の解釈、距離の測り方）と、所要時間としての距離の定義を追記

## [v1.2] — 2026-09-09

課題ごとの URL、端末への保存（Service Worker）、選択の追随とフライ・トゥ。PC も点のクリックでカードを出す方式に。

### サイト
- Service Worker（`docs/sw.js`、生成物）を導入。取得した詳細データ・座標・Plotly を端末に保存し、再訪時は通信なしで開く（オフラインでも動く）。データが更新されると自動で古い保存を捨てる。HTML はネットワーク優先
- 右下の「全データ読み込み完了」は 2 秒で消す（従来 4 秒）。端末保存から一瞬で読み込めた場合は表示しない。「（検索が使えます）」の文言を削除
- **課題ごとの URL**: `map2d/?award=課題番号`（3D・球面も同じ形）で開くと、その課題にズーム／カメラを向けてリングとカードを表示。該当する詳細データ 1 片だけ読むので全件の読み込み完了を待たない
- カードにリンクのコピーと共有のアイコンボタン（共有は対応端末のみ。コピー後は 1.5 秒チェック印に）。カードを開くとアドレスバーの URL もその課題のものになる
- **フライ・トゥ**: URL や検索結果で課題を開くと、全体図の上でリングとカードを出してから 1.2 秒かけて周辺が見える倍率（2D は ±2.5、3D は注視点に寄る、球面は正面へ大円に沿って回る）まで連続的に寄る
- カードに「全体 ⇄ 周辺」の切替ボタン（球面は「この点を正面に」）
- **地図を動かしても選択は消えない**: リングは点に追随（球面の裏側に回ったら隠す）。PC のカードは点に付いて動き、スマホのカードは固定。解除は空白のタップ／クリックと ×、Esc
- **PC でも点のクリックで詳細カード**（KAKEN はカードのクリックで開く。従来の「クリックで即 KAKEN」から変更）。検索結果のクリックも PC でカードを出す

## [v1.1] — 2026-09-09

主にスマホでの使い勝手と、ビュー間のデータ共有。

### サイト
- 詳細データ（タイトル・キーワード等）を課題番号順の 1 セット `docs/shards/` にまとめて 3 ビューで共有。ビューを切り替えても再読み込みしない（ブラウザキャッシュが効く）。リポジトリの詳細データは 156MB → 52MB
- 検索欄の右端に × ボタン（検索語・地図上の強調・結果窓をすべて消す）。結果窓の外をクリック／タップすると結果窓だけ閉じ、入力欄に戻ると再表示（3 ビュー共通）
- 検索結果をタップ／クリックしたとき: タッチ端末は結果窓を閉じてから点を表示。3D・球面はカメラをその点に向け（球面は正面に、3D は注視点を移して寄る）、点の実際の画面位置にリングとカードを出す。PC の 3D・球面もカメラが動くようになった（従来は無反応）
- スマホで検索欄にフォーカスしたときの画面の自動拡大を抑止（タッチ端末では入力欄の文字を 16px に。iOS Safari の仕様対策）
- スマホのヘッダーが幅に収まるように: 検索欄が先に縮む（ボタンははみ出さない）、「操作」ボタンは羅針盤メニューの末尾「操作の説明」へ移動
- スマホの「種目」を「大区分」と同じボトムシートに統一（行タップで表示切替、すべて表示／非表示。大区分の行だけ色の丸付き）。「のみ」ボタンは廃止し、一度すべて非表示にしてから必要なものを表示する方式に
- スマホの詳細カードを画面の下端に（以前あった下部の大区分ボタン分の余白 64px が残っていたのを 12px に）。上側に出すのは、点のリングが下端カードに実際に掛かるときだけ（安全領域は実測）
- スマホの 3D・球面で拡大しているときの回転が速すぎる問題: 1 本指回転の感度を「視点から面（球面）／注視点（3D）までの距離」に比例させ、寄るほど弱く。慣性も同じ感度に

## [v1.0] — 2026-09-08

初版として公開中の状態。2026-09-04 の公開から 09-08 までの改良を含む。

### 地図データ
- 対象: 2019–2025 年度開始の科研費採択課題 206,078 件（採択時概要あり・不採択除く）
- 埋め込み: cl-nagoya/ruri-v3-310m（768 次元、L2 正規化）。テキスト＝タイトル＋キーワード＋採択時概要
- 配置: UMAP（cosine, n_neighbors=15, seed=42）。2D/3D は min_dist=0.1、球面は haversine・min_dist=0.0・spread=0.3
- 配色: 大区分 11 色の意味順色相環。通称は正典（審査区分表）どおり C=工学、D=応用物理・材料工学、E=化学（09-07 修正）

### サイト
- 入口ページ: 点群を背景にした明背景デザイン、2D／3D／球面の 3 ビューへの入口
- 3 ビュー共通: タイトル・キーワード・課題番号検索、大区分の表示切替、点から KAKEN 課題ページへ
- 2D: 種目フィルタ、なげなわ／矩形選択の集計（件数・区分内訳・頻出キーワード）
- 3D／球面: 自由回転、球面は端のない地球儀
- PC: ホバーで概要、クリックで KAKEN。ドラッグ・ホイール中は吹き出しを消す
- タッチ端末: タップ→カード→カードのどこでも KAKEN。3D／球面は自前のカメラ制御（1 本指回転・2 本指ピンチ）、回転の慣性
- ヘッダー左の羅針盤アイコンからトップと他ビューへ移動（ウェブアプリモード対応）
- PWA（ホーム画面追加）、羅針盤アイコン、OGP 画像、JSON-LD・sitemap・robots・llms.txt
- 二段階読み込み（座標 → 詳細シャード）、GoatCounter による閲覧計測

### 文書
- README（利用規程・ライセンス・引用）、開発者向け技術ノート `doc/method.md`（設計の理由・参考文献）
